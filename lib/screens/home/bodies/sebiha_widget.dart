import 'package:flutter/material.dart';

class SebihaWidget extends StatelessWidget {
  const SebihaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
    );
  }
}